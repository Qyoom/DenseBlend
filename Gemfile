# frozen_string_literal: true

source "https://rubygems.org"

git_source(:github) {|DenseBlend| "https://github.com/#{DenseBlend}" }

# gem "rails"

gem "jekyll"
# gem 'github-pages', group: :jekyll_plugins

group :jekyll_plugins do
  gem 'jekyll-sitemap'
  gem 'jekyll-feed'
  gem 'jekyll-seo-tag'
end
